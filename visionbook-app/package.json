{
  "name": "visionbook",
  "version": "1.0.0",
  "private": true,
  "description": "Bundled Android WebView app for Scaling Out AI.",
  "scripts": {
    "prepare:web": "node scripts/prepare-web.mjs",
    "sync": "npm run prepare:web && cap sync android",
    "build:android": "npm run sync && node scripts/build-android.mjs",
    "copy:apk": "node scripts/copy-apk.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4"
  }
}
